import Layout from '../../components/layouts/index'
import Loading from '../../components/Loading'

function Projects() {
    return (
        <Layout> 
            <h1> Zde budou projekty </h1>
        </Layout>
      );
}

export default Projects;